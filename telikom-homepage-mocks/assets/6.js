(()=>{'use strict';const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const nav=document.querySelector('.nav');addEventListener('scroll',()=>nav&&nav.classList.toggle('scrolled',scrollY>40),{passive:true});const slides=[...document.querySelectorAll('.hero-bg')],dots=[...document.querySelectorAll('.hero-dot')];let slideIndex=0;function showSlide(i){slideIndex=(i+slides.length)%slides.length;slides.forEach((s,n)=>s.classList.toggle('active',n===slideIndex));dots.forEach((d,n)=>d.classList.toggle('active',n===slideIndex));}dots.forEach((d,i)=>d.addEventListener('click',()=>showSlide(i)));if(!reduce&&slides.length)setInterval(()=>showSlide(slideIndex+1),5200);const reveal=[...document.querySelectorAll('.reveal')];if('IntersectionObserver'in window&&!reduce){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target);}}),{threshold:.12});reveal.forEach(el=>io.observe(el));}else reveal.forEach(el=>el.classList.add('in'));const story=document.getElementById('storyScene'),storyImg=story&&story.querySelector('img');function storyParallax(){if(!story||!storyImg||reduce||innerWidth<721)return;const r=story.getBoundingClientRect(),p=Math.max(0,Math.min(1,(innerHeight-r.top)/(innerHeight+r.height)));storyImg.style.transform=`translate3d(0,${(p-.5)*36}px,0) scale(1.04)`;}addEventListener('scroll',storyParallax,{passive:true});const shell=document.getElementById('railShell'),rail=document.getElementById('rail'),progress=document.getElementById('railProgress');let railX=0;function maxRail(){return rail?Math.max(0,rail.scrollWidth-innerWidth+90):0}function paintRail(){if(!rail)return;if(innerWidth<=1100){rail.style.transform='';return}const max=maxRail();railX=Math.max(0,Math.min(max,railX));rail.style.transform=`translate3d(${-railX}px,0,0)`;if(progress)progress.style.width=(max?railX/max*100:0)+'%'}function railActive(){if(!shell||innerWidth<=1100)return false;const r=shell.getBoundingClientRect();return r.top<innerHeight*.72&&r.bottom>innerHeight*.28}addEventListener('wheel',e=>{if(!railActive())return;const max=maxRail();if(max<=0)return;const forward=e.deltaY>0,back=e.deltaY<0,canForward=railX<max-1,canBack=railX>1;if((forward&&canForward)||(back&&canBack)){e.preventDefault();railX+=e.deltaY*1.05;paintRail()}},{passive:false});addEventListener('resize',paintRail);paintRail();addEventListener('load',()=>{try{if(!window.THREE||reduce)return;const canvas=document.getElementById('heroCanvas');if(!canvas)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(45,innerWidth/innerHeight,.1,100),renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:innerWidth>800});camera.position.z=8;renderer.setPixelRatio(Math.min(devicePixelRatio,innerWidth<720?1:1.5));renderer.setSize(innerWidth,innerHeight);const group=new THREE.Group();scene.add(group);group.add(new THREE.Points(new THREE.SphereGeometry(2.2,56,56),new THREE.PointsMaterial({color:0x72ddff,size:.025,transparent:true,opacity:.55})));group.add(new THREE.Mesh(new THREE.SphereGeometry(2.14,30,30),new THREE.MeshBasicMaterial({color:0x39aee6,transparent:true,opacity:.08,wireframe:true})));for(let i=0;i<11;i++){const a=Math.random()*Math.PI*2,b=(Math.random()-.5)*Math.PI*.8,a2=Math.random()*Math.PI*2,b2=(Math.random()-.5)*Math.PI*.8,p1=new THREE.Vector3(2.2*Math.cos(b)*Math.cos(a),2.2*Math.sin(b),2.2*Math.cos(b)*Math.sin(a)),p2=new THREE.Vector3(2.2*Math.cos(b2)*Math.cos(a2),2.2*Math.sin(b2),2.2*Math.cos(b2)*Math.sin(a2)),mid=p1.clone().add(p2).multiplyScalar(.5).normalize().multiplyScalar(3),curve=new THREE.QuadraticBezierCurve3(p1,mid,p2);group.add(new THREE.Line(new THREE.BufferGeometry().setFromPoints(curve.getPoints(34)),new THREE.LineBasicMaterial({color:0x6fdcff,transparent:true,opacity:.3})))}group.position.x=innerWidth<900?1.2:2.5;let mx=0,my=0;addEventListener('mousemove',e=>{mx=(e.clientX/innerWidth-.5)*.3;my=(e.clientY/innerHeight-.5)*.16},{passive:true});function render(){group.rotation.y+=.0018;group.rotation.x=.08+my;group.rotation.z=mx*.12;renderer.render(scene,camera);requestAnimationFrame(render)}render();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight);group.position.x=innerWidth<900?1.2:2.5})}catch(err){console.warn('3D layer skipped:',err)}})})();